{% trans_default_domain 'FOSUserBundle' %}


<div class="row">

    <div class="col-lg-8">
    	<form id="form" action="{{ path('fos_user_resetting_send_email') }}" method="POST" class="form-inline"  novalidate>

	        <label for="username">{{ 'resetting.request.username'|trans }}</label>
	        <input type="text" id="username" name="username" required="required" class="form-control"/>

	        <input type="submit" value="{{ 'resetting.request.submit'|trans }}" class="btn-primary btn btn-small ml-2" id="btn_guardar"/>

    	</form>	
    </div>

</div>

