{% extends 'base.html.twig' %}

{% block body %}
    <h1>Detalleventum</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ detalleVentum.id }}</td>
            </tr>
            <tr>
                <th>Cantidad</th>
                <td>{{ detalleVentum.cantidad }}</td>
            </tr>
            <tr>
                <th>Subtotal</th>
                <td>{{ detalleVentum.subtotal }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('detalleventa_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('detalleventa_edit', { 'id': detalleVentum.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
